class Barrier extends BackgroundObject {
    width = 580;
    height = 200;

    constructor(imgPath, x) {
        super(imgPath, x);
        // this.loadImage(imgPath);
        // this.x = x;
    }
}